<!--
SPDX-License-Identifier: CC-BY-SA-4.0
Copyright (c) 2026 wUwproject
-->

**《母书答"为什么"，架构答"怎么做"——〈我思故我写 · 架构解析〉成册说明》**

AI 参与披露：本文及所述册子为作者 wUwproject 与 AI 协作完成——AI 参与核对、整理、成文与校验，思考方向、判断标准与取舍意志由作者负责。册子整体采用 CC BY-SA 4.0 协议，可自由传播，须署名。

我出了一册新书——《我思故我写 · 架构解析——七套核心系统的工程实现》。约 5.0 万字，12 章，已全部公开，免费阅读与下载。

先说清楚它不是什么：它不是方法论续作，不是母书第二卷，也不是排版解析的续篇。母书《我思故我写》（v1.2.0 完结版，35 章）保持完结不动；排版解析册收录的是排版文档——同一批 markdown 怎么从一篇源稿变成一本 35 章的书；这一册是结构侧的姊妹卷，收录的是工程文档——wUwproject 的技能与智能体，模块怎么分、链路怎么走、配置体系与关键机制怎么设计。

一、为什么出这一册

母书回答"为什么"：约束为什么是加法、穷举为什么是宿命、判断为什么留在人手里。但原则不落地就只是格言——母书附录 B 工具索引是索引，不是实现。这一册把索引展开成结构：把《我思故我写》方法论里那些结论（递归自举的元验证、槽位是减法、配置推动、链驱动 Pipeline 编排……）在真实工程里的落地形态，逐篇写成架构文档。

仓库里现有 14 篇架构文档，本册收录其中 **8 篇**——不是按"写了就收"，而是按"是否代表方法论当前的核心落地"取舍：成熟 5 篇（skill-standardization、semantic-split、activity-duration-estimation、rag-assistant、structured-writer）+ 实验性 2 篇（orchestrator、silprespec-orchestrator）+ 演进收束篇 1 篇（《技能编排器到 agent 编排器——编排对象迁移与"圈"的 z 轴》，架构 08，以编排器两代为标本的论述篇，非系统）。其余 6 篇（skill-function-test、analysis-toolkit、round-robin-allocator、latex-modular、novel-weaver、local-rag-builder）保持仓库文档形态，随各自项目演进，不因未入书而降级维护。

架构文档有一个不同于母书/排版册的特点：**它随代码走，不随书走**。工具每迭代一个版本，文档就可能要改——所以各篇版本号跟随对应工具的真实版本（如 skill-standardization v2.103.0、rag-assistant v2.4.1），册子只保证"成册时刻文档与代码一致"，不承诺永远正确。

二、收录了什么

12 章：版权页 + 导读（方法论 ↔ 架构映射 + 七系统关系总览 + 演进收束篇定位）+ 架构 01~08 + 附录 A 统一术语表 + 附录 B 运行速查：

| 章 | 系统 | 类别 | 一句话 |
|---|---|---|---|
| 架构 01 | skill-standardization（标准化审计引擎） | 成熟 | 引擎审计别的技能，自身配置纳入同一审计体系 |
| 架构 02 | semantic-split（语义拆分规划） | 成熟 | 触发双清单——槽位四维展开的工程形态 |
| 架构 03 | activity-duration-estimation（活动历时估算） | 成熟 | 三点估算/蒙特卡洛的工具化；配置推动 |
| 架构 04 | rag-assistant（本地知识库智能体） | 成熟 | LLM 分词穷举组合检索 + 门禁校验；top-N 多 KB 路由 |
| 架构 05 | structured-writer（结构化写作引擎） | 成熟 | 配置推动的穷举一致性（模板 meta 数组 = 统一推动点位） |
| 架构 06 | orchestrator（第 I 代编排器） | 实验性 | 预定义技能链 + LLM 只做两头 |
| 架构 07 | silprespec-orchestrator（第 II 代前置规范编排器） | 实验性 | 14 种穷举原子化组合，PY 确定性查表，LLM 填空执行 |
| 架构 08 | 演进收束篇（编排对象迁移论述） | 论述 | 迁移的是对象与通道；表象逼近 ReAct、内核仍有限枚举、决策在人；"圈"沿拟人 z 轴上移但有界 |

7 个系统不是并列的——导读用一张三层总览图说明调用关系：技能层（宿主平台加载，无独立进程，可被编排器 subprocess 调用）→ 编排器层（第 I 代 subprocess 跑技能、第 II 代 HTTP 调智能体）→ 智能体层（独立 HTTP 服务，自带 Web UI）。技能与智能体是两类完全不同的存在，附录 B 把七套系统的运行方式列成一页速查。架构 06、07 标注**实验性**：代码已实现，端到端实验验证仍在进行，架构可能随实验结果调整——它们代表方法论当前正在探索的方向：从"单系统内的前置规范"走向"多系统编排层的前置规范"。

架构 08（演进收束篇）不在三层里：它不新增系统，而是把架构 06 → 07 之间空着的变化线立成专文。两代编排器各自成篇、各有一节"与上一代的关键差异"，但没有一篇回答"为什么从编排技能走到编排智能体、这一步改了什么、什么没变"。架构 08 以 Orchestrator 与 silprespec 为标本给出回答：迁移的是编排对象与调用通道（subprocess 跑技能脚本 → HTTP POST 调智能体 API）；silprespec 表象逼近 ReAct（每步 LLM 在场、有循环、会调工具、有进度记录）但绝不是 ReAct——路径空间被事前穷举钉死，LLM 的"编排"是圈内填空；决策依旧在人。它把这种关系画成"圈"的 z 轴：编排动作的承担者沿拟人轴从"人工编排"上行到"LLM 自主编排的表象"，但同一个圈的圆心（真决策）与边界（枚举/回退/终止）从未离开人的桌面——z 上行有界，未越过"决策让渡"的临界线。

三、成册前做了两件该做的事：核对同步、样式统一

架构文档跟着代码写，最容易与代码脱节。arch-v1.0.0 成册前把 7 篇与对应工具/智能体的真实代码逐篇核对：rag-assistant 补 top-N 多 KB 路由与 pypdfium2 引擎迁移、structured-writer 补小说线配置统一源、skill-standardization/activity-duration-estimation 版本头对齐——文档落后于代码的地方全部修正。

arch-v1.0.1 做的是另一件事：**内容样式统一**。7 篇文档来自不同时期，入书时各篇尾部带着不同的生成戳记（「最后更新」脚注、"本文档基于 XX 版 SKILL.md 综合分析整理"）与正文分隔线——成书里七篇尾部样式不一，戳记里的版本还会过期（曾出现文档内版本与导读映射表脱节）。这一版把清洗全部收进拼接脚本：章头摘要/日期戳、章尾两类戳记、正文分隔线自动剥离，成书层级统一由标题承担。规则固化在 `STRUCTURE_GUIDE.md`，产物逐篇实证扫描验证，不再依赖手工。

arch-v1.1.0 补了第三件事：**立一条空着的变化线**。架构 06/07 是两篇并列的系统文档，导读给了代际定位（第 I/II 代），但"为什么从编排技能走到编排智能体"没有专文——新增《技能编排器到 agent 编排器——编排对象迁移与"圈"的 z 轴》（架构 08，演进收束篇）：迁移的对象与通道、表象逼近 ReAct 的四项可见特征与四项内核差异、"圈"沿拟人 z 轴上移而圆心与边界不动（决策让渡是判据）。该篇直接呼应导读的实验性承诺（"母书如出后续版本，亦将收录其沉淀出的方法论结论"）——先在架构册侧把实证篇立住，方法论侧等母书自行演进。新增后正文 8 篇（系统仍七套）、12 章。

arch-v1.1.1 是紧随其后的文意修订（纯表述，不增篇/章）：把"圈"的 z 轴正解立成规范表述——**同一个圈整圈沿拟人轴上移一个坐标，圈还是那个圈、位置不同**（不是轴上套圈，套圈是把平面内循环嵌套误读成立体叠加）；同时把三组讨论中形成的术语收进词汇对照并全册统一：**loop→循环、自由路径决策→自主编排、填槽范式→填槽即决策（逐句）**，并明确"循环的每一圈都产生决策，不是让 LLM 空转一圈"。引用表补 [11] 原始对话条目（z 轴正解与三组术语的提出语境）；导读、术语表（拟人 z 轴条目改述 + 新增自主编排/填槽即决策两条）、README/index.html/PDF 封面/STRUCTURE_GUIDE 同步至 arch-v1.1.1。

arch-v1.1.2 是紧随其后的印刷字体事故根因修复（纯运维/排版，不增篇/章）：make_pdf.py 改双 @font-face 注册 SourceHanPrint（思源黑体 CFF）+ SourceHanMono（思源等宽 CFF）并以 !important 全元素覆盖，根治 Chromium page.pdf() 因 CFF 与本地 TTF 混排触发的正文 NSimSun 级联（v1.1.1 旧 PDF 120 页正文回退 NSimSun 73 页/60%）；同时把思源未覆盖的 3 个 unicode 符号字符内容层替换（勾选符 U+2611 → √、空框 U+2610 → □、缩进箭头 U+21B3 → →），彻底杜绝 SegoeUISymbol/CambriaMath 等系统回退字体进入 PDF。新 PDF 95 页（封面 + 94 正文）页码/目录/书签齐全，全书 50338 spans 全部 Type3 思源轮廓嵌入（Helvetica 仅页码层标准字体），零微软版权字体分发。导读、术语表、README、index.html、PDF 封面、STRUCTURE_GUIDE 同步至 arch-v1.1.2。

arch-v1.1.3 是 Chromium page.pdf() 整档缩放根因修复（纯运维/排版，不增篇/章）：v1.1.2 沿用 chromium 把"print 布局超内容盒（~672px@96dpi = A4 794px − 18/16mm 边距）"的元素等比缩放整本的机制——架构册 4 级工程文件树行宽 1185px 触发 ×0.667，正文 12pt→8pt（h1 20.4→13.6），正文密度被假性放大 2.25× 倍；母书 OCEAN 五维树 866px 触发 ×0.892（同机制，10.7pt），排版书最大块 794px 不触发，作为对照。修复按"消除全部触发源而非补偿缩放"分四步：(1) PRINT_CSS 让 body 左对齐并收进 665px（不再 margin:auto 居中——居中偏移使满宽块右缘 729.5px 仍触发缩放）；(2) 全局 box-sizing:border-box（.toc 等 padding 元素 width:auto 不再超出 body 内容盒）；(3) 表格 width:100% + 单元格 overflow-wrap / 行内 code / 流程文本标签（.edge-fall 等）print 态 white-space:normal——white-space:pre 下 overflow-wrap 无效，长 token 必须改 normal 才会断行；(4) 残余硬溢出（等宽 nowrap 树行）在 print 媒体下按需 zoom（预算 665）——zoom 须在 emulate_media('print') 后执行，screen 与 print 布局宽度不同（pre→pre-wrap 等规则生效差异），screen 态测量会与 page.pdf() 实际布局错位（曾测 37 棵误缩/漏缩 → print 态正确 15 棵）。实测：正文 12.0pt / h1 20.4pt / h2 16.8pt / toc 11.0pt / code 9.5pt，零超宽元素，无全局缩放；树形图 zoom 等比缩框线对齐保持，表格四边封闭（Q2 fix 在 ab42d28 移植的 _split_row + 表格 100% 收进），长流程标签换行不溢出。同步页数变化：8pt→12pt 正文密度降为 ~1/2.25，新 PDF 159 页（封面 + 正文 158）；导读、README、index.html、PDF 封面、00_版权页、STRUCTURE_GUIDE 同步至 arch-v1.1.3。母书按决策维持不动（10.7pt ≈ 印刷五号，可接受；修复对其无收益且风险大）。

arch-v1.1.4 是架构 04（rag-assistant）的文档同步与两个解析入篇（纯文档修订，不增篇/章）：文档对照代码 v2.4.1 全文复核——补录长期缺失机制（决策循环两阶段：模式判定单次调用 + 动作校验循环 max_retries=5、禁止逃逸动作模式；evidence 原文锚定：key 精确一致 + value 单连续子串 + 三源校验 + NLI verify 语义二次判断；会话管理六端点与 max_sessions 自动归档；Web 端点表补全；引用门禁插件来源标记），并新增两节解析：§1.4「可配置的前置分类——三元分类下的专属场景有限枚举」（无定义穷举经三槽位骨架与查询类型场景两级收敛为有限枚举——归类切维度、场景约束值域）与 §3.10「Ranker 后插件点位——before_response 旁路扩展（已实现）」（input_return 插件执行于完整检索管道含 rerank 之后、综合回答之前：附加注入不改写、失败透传、五道防线复用，主体变更必须架构级——用点位隔离代替试错）。设计原则表、概览图、生命周期、§5.3 动作协议、记忆系统、术语表（新增四条）同步。全册字数 45,620 → 49,841（约 5.0 万字），12 章不变；新 PDF 167 页（封面 + 正文 166）。导读、术语表、README、index.html、PDF 封面、00_版权页、成册说明同步至 arch-v1.1.4。


arch-v1.1.5 是 PDF 目录页码列规范同步（纯排版修复，不增篇/章）：母书/排版书 v1.2.0 修订（STYLE_GUIDE 11.4 目录页码列铁律：页码基线 x=538 / 虚线终点 ≤521 / link_rect 右缘 543）此前漏移植架构册，三位数页码右缘穿 .toc 装饰右边框 4.6pt。本次同步三项数值并补齐跨行续行合并（MERGE_GAP=23pt）、纯数字页码行剔除、顺序锚定全局回退兜底。fitz 门禁扫描 103 条页码右缘最大 538.0、0 穿框 PASS，书签 103 条无回归，167 页不变。三本姊妹书自此共用同一套目录/排版规范，差异仅剩各书配置。

四、导读和附录

导读有两件事：一张方法论 ↔ 架构映射表（每篇架构对应母书哪些篇目的落地，如架构 01 对应母书 03 递归自举的元验证、架构 04 对应 06 领域智能体 + 09a 探测边界 + 09c 语义免疫），和一张七系统关系总览（技能层/编排器层/智能体层三层调用图，两代编排器调的对象不同：第 I 代 subprocess 真跑技能脚本、第 II 代 HTTP POST 调智能体 API）。

附录 A 统一术语表：七篇共用概念（前置规范、槽位、配置推动、编排器/技能/智能体边界……）收拢成一页。承自母书的术语定义与母书一字不差，不另造定义；架构册特有的工程术语单列并注明使用范围。附录 B 运行速查：七套系统运行方式一页速查——技能由宿主平台加载（无端口），编排器与智能体是独立程序（main.py 起进程、占端口、带 Web UI），端口表与配置位置可查。

五、一份结构化设计的判据

这个系列三册各答一问：母书管"为什么"，排版册管"怎么排"，本册管"怎么做"。落到"怎么做"的判据就一句话——**结构先于实现，实现反哺结构**。skill-standardization 用配置驱动而非硬编码黑白名单，因为无法枚举所有情况；semantic-split 用 bge 语义匹配而非纯规则，因为语言没有穷尽；structured-writer 把事实自检做成内嵌标记（零额外调用），因为工程要省 token 也要可验证。这些取舍各有因果，正文里逐篇展开。

六、获取（全部免费）

| 方式 | 入口 |
|---|---|
| 在线阅读整本册子（GitHub Pages） | https://ldxs001.github.io/architecture/ |
| 下载 PDF 打印版 / HTML / EPUB（发行版 arch-v1.1.5） | https://github.com/Ldxs001/architecture/releases/tag/arch-v1.1.5（Gitee 同 tag，含 PDF 打印版） |
| 册子源码 + 构建管线 + 入书规范（可溯源） | https://gitee.com/wUwproject/architecture（STRUCTURE_GUIDE.md / 顶层架构文档 / book/ 构建管线） |

提示：GitHub 在国内访问不稳定，国内读者建议从 Gitee 获取源码与发行版；GitHub Pages 在线阅读适合国际线路或网络通畅环境。

协议：册子整体 CC BY-SA 4.0（署名-相同方式共享），须署名 wUwproject 并按相同协议共享。源仓库双平台同步，内容一致。

七、谁会需要它

维护或使用 wUwproject 技能的：这 8 篇是这些工具的设计文档——为什么这么分模块、配置体系怎么长出来的、边界在哪（架构 08 收束篇为论述而非系统文档），比读代码快。
在用 AI 搭自己的工具链的：技能/智能体/编排器三层怎么分、配置推动怎么落地、两代编排器为什么调不同的对象——本册给出真实工程的取舍样本。
读过母书的：你看到过"槽位是减法""配置推动"这些结论，本册给你工程侧的落地形态——同一套方法论在真实系统里长什么样。

它不吹成熟：实验性两篇明晃晃标着"验证进行中"；它不承诺正确：文档随工具迭代更新，册子只对成册时刻负责；它也不提供新结论——结论在母书里，版式在排版册里，这里只有结构。

Cogito, Scribo. 我思，故我写。这一册是"写"落进结构里的那一部分。
